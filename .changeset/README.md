# Changesets

This folder is managed by
[`@changesets/cli`](https://github.com/changesets/changesets). Changesets record
user-visible changes and drive version pull requests and Git tags.

Create a changeset with:

```bash
npm run changeset
```
