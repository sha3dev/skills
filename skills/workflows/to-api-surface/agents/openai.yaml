interface:
  display_name: "To API Surface"
  short_description: "Specify and build a Fastify API surface"
  default_prompt: "Use $to-api-surface to interview me about and build an API application surface."
policy:
  allow_implicit_invocation: false
