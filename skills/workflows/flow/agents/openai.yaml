interface:
  display_name: "Flow"
  short_description: "Continue through the right project workflow"
  default_prompt: "Use $flow to continue this project."
policy:
  allow_implicit_invocation: false
