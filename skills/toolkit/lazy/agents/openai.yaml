interface:
  display_name: "Lazy"
  short_description: "Force the smallest correct implementation"
  default_prompt: "Use $lazy to solve this task with the laziest correct implementation."
policy:
  allow_implicit_invocation: false
